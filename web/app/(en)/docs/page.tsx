import { DocPage } from "@/components/docs/doc-page";
import { docMetadata } from "@/lib/seo/metadata";

export const metadata = docMetadata("en", "");

export default function DocumentationPage() {
  return <DocPage docKey="" locale="en" />;
}
